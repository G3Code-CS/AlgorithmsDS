class Node {
  constructor() {
    this.value = value;
    this.node = null;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
    this.length = 0;
    this.tail = null;
  }

  push(value) {}

  pop() {
    return value;
  }

  _find(value, test = this.test) {}

  _test(a, b) {}
  delete(index) {}
  get(index) {}
  testIndex(search, __, i) {
    return search === i;
  }
}
